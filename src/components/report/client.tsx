"use client";

import { useParams, useRouter } from "next/navigation";

import ReportsForm from "@/components/report/reports-form";

const Client = () => {
  // console.log("client", data);
  return <ReportsForm />;
};

export default Client;
